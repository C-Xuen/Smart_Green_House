# 智能温室监控系统 — 开发说明文档

## 一、系统架构总览

```
┌─────────────────────────────────────────────────────────────┐
│                    BearPi-Pico H3863 开发板                   │
│                                                             │
│  ┌──────────┐  ┌──────┐  ┌──────┐  ┌───────┐  ┌─────────┐ │
│  │ DHT11    │  │ JW01 │  │ ADC  │  │ Flame │  │ Buzzer  │ │
│  │ 温湿度   │  │ CO2  │  │土壤光│  │ 火焰  │  │ 蜂鸣器  │ │
│  │ GPIO_00  │  │ UART2│  │CH2/3 │  │GPIO_01│  │ GPIO_02 │ │
│  └────┬─────┘  └──┬───┘  └──┬───┘  └──┬────┘  └────┬────┘ │
│       │           │         │         │            │       │
│  ┌────┴───────────┴─────────┴─────────┴────────────┴─────┐ │
│  │              SGH 主控任务 (LiteOS RTOS)                 │ │
│  │  采集 → OLED显示 → 按钮交互 → 蜂鸣器告警               │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐ │
│  │ WiFi 连接    │  │ HTTP 服务器  │  │ MQTT 上传         │ │
│  │ 连接热点     │  │ 端口 80      │  │ 华为云 IoTDA      │ │
│  └──────────────┘  └──────┬───────┘  └────────┬──────────┘ │
└───────────────────────────┼───────────────────┼────────────┘
                            │                   │
              ┌─────────────┘                   └──────────────┐
              ▼                                                ▼
   ┌──────────────────────┐                    ┌──────────────────────┐
   │ Flask app.py (PC)    │                    │ 华为云 IoTDA (云端)  │
   │ 读取设备 HTTP API     │                    │ 设备影子存储数据     │
   │ 提供 Web 页面        │                    │ 消息跟踪 / 规则引擎  │
   └──────────┬───────────┘                    └──────────────────────┘
              │
    ┌─────────┴─────────┐
    ▼                   ▼
┌──────────┐    ┌─────────────────┐
│ 浏览器   │    │ 微信小程序      │
│ 网页访问 │    │ wx.request 调用 │
└──────────┘    └─────────────────┘
```

---

## 二、硬件引脚分配

| 模块 | 功能 | 引脚 | 信号类型 |
|------|------|------|---------|
| DHT11 | 温湿度传感器 | **GPIO_00** | 单总线数字（OneWire） |
| JW01 | CO2 浓度传感器 | **TX=7, RX=8** | UART2, 9600 8N1 |
| ADC CH2 | 土壤湿度传感器 | 模拟输入 | ADC 采样 → 电压值 |
| ADC CH3 | 光敏电阻 | 模拟输入 | ADC 采样 → 电压值 |
| Flame | 火焰传感器 | **GPIO_01** | 数字输入（低电平触发） |
| Buzzer | 蜂鸣器 | **GPIO_02** | 数字输出（高电平响） |
| BTN1 | 翻页按钮 | **GPIO_03** | 数字输入（低电平按下） |
| BTN2 | 增加按钮 | **GPIO_14** | 数字输入（低电平按下） |
| BTN3 | 减少按钮 | **GPIO_06** | 数字输入（低电平按下） |
| OLED | 显示屏 | I2C | 128x64 SSD1306 |

---

## 三、各传感器采集方式

### 3.1 DHT11 温湿度（数字单总线）

- **原理**：DHT11 通过单根数据线返回 40 位数据（湿度整数+小数+温度整数+小数+校验和）
- **读取流程**：
  1. 主机拉低总线 20ms（启动信号）
  2. DHT11 响应：拉低 80us → 拉高 80us
  3. DHT11 发送 40 位数据，每位以 50us 低电平 + 高电平长度区分 0/1
  4. 校验和验证
- **API**：`dht11_read()` → `dht11_get_temp_int()` / `dht11_get_humi_int()`

### 3.2 JW01 CO2 传感器（UART）

- **原理**：通过 UART 串口每 1 秒主动发送一帧 6 字节数据
- **帧格式**：`0x2C` + 浓度高位 + 浓度低位 + 保留 + 保留 + 校验和
- **CO2 值** = `frame[1] × 256 + frame[2]`
- **UART 配置**：波特率 9600，8 数据位，1 停止位，无校验
- **API**：`jw01_read()` → `jw01_get_co2()`

### 3.3 ADC 传感器（土壤湿度 + 光敏）

- **原理**：芯片内置 12 位 ADC，采样频率 500KHz
- **CH2（土壤湿度）**：电压越高 → 湿度越低，换算公式 `soil% = mv × 100 / 3300`
- **CH3（光敏电阻）**：电压越低 → 光照越强，换算公式 `light% = (3300 - mv) × 100 / 3300`
- **采样方式**：自动扫描模式，回调函数保存结果
- **API**：`adcsensor_sample()` → `adcsensor_get_voltage_ch2()` / `ch3()`

### 3.4 火焰传感器（GPIO 数字量）

- **原理**：检测到火焰时输出低电平
- **API**：`flame_detected()` — 返回 1 = 检测到火焰

### 3.5 蜂鸣器（GPIO 输出）

- **原理**：GPIO 输出高电平驱动蜂鸣器
- **触发条件**：火焰检测到 **或** 任一传感器超阈值
- **API**：`bee_on()` / `bee_off()`

### 3.6 按钮（GPIO 输入 + 消抖）

- **原理**：独立任务每 10ms 扫描 GPIO 电平，检测下降沿
- **功能**：BTN1 翻页、BTN2 加阈值、BTN3 减阈值
- **API**：`btn1_pressed()` / `btn2_pressed()` / `btn3_pressed()`

---

## 四、MQTT 传输详情

### 4.1 连接配置

| 参数 | 值 | 说明 |
|------|-----|------|
| Broker | `tcp://049a9b92ef.iotda-device.cn-south-4.myhuaweicloud.com:1883` | 华为云 IoTDA MQTT 接入点 |
| ClientId | `设备ID_H3863_0_0_时间戳` | 参数生成工具获取 |
| Username | `设备ID_H3863` | 设备标识 |
| Password | 64 位 hex 字符串 | 参数生成工具获取 |
| 心跳 | 120 秒 | keepAliveInterval |
| QoS | 0 | 属性上报 |

### 4.2 上报 Topic

```
$oc/devices/{设备ID}/sys/properties/report
```

### 4.3 上报 JSON 格式（华为云标准）

```json
{
  "services": [{
    "service_id": "smart_green_house",
    "properties": {
      "temperature": 27.5,
      "humidity": 49.2,
      "co2": 450,
      "soil_moisture": 48,
      "light": 65,
      "flame": 0
    }
  }]
}
```

### 4.4 上报频率

每 5 秒发送一次。传感器采集在独立任务中并行运行。

### 4.5 库依赖

| 库 | 用途 |
|----|------|
| Paho MQTT C Client | MQTT 客户端（`MQTTClient.h`） |
| cJSON | JSON 序列化（`cJSON.h`） |
| 初始化 | 必须先调 `MQTTClient_init()` 创建内部互斥锁 |

---

## 五、网页启动方式

### 5.1 设备端 HTTP 服务器（server.c）

**运行方式**：RTOS 独立任务，优先级 18，栈 8KB。

**端口**：80

**接口**：
| 路径 | 说明 |
|------|------|
| `GET /` | 返回内嵌 HTML 页面（温湿度监控卡片） |
| `GET /api/temp_humi` | 返回 JSON 传感器数据 |

**JSON 响应示例**：
```json
{"temperature":27.5,"humidity":49.2,"co2":0450,"soil_moisture":1024,"light":2048}
```

### 5.2 Flask 网页服务器（app.py，PC 端运行）

**启动方式**：
```bash
pip install flask requests
python app.py
```
监听 `0.0.0.0:5000`，同一 WiFi 下任意设备可访问。

**数据来源**：调用华为云 IoTDA REST API，读取设备影子的 `reported.properties`。

**接口**：
| 路径 | 说明 |
|------|------|
| `GET /` | 渲染 `templates/index.html`（ECharts 仪表盘） |
| `GET /api/data` | 返回传感器数据 JSON |
| `GET /api/history` | 温湿度历史（最近 30 条） |
| `GET /api/alarms` | 告警记录 |
| `GET/POST /api/threshold` | 读取/修改阈值 |

**网页访问**：
- 本地：`http://127.0.0.1:5000`
- 局域网：`http://<PC_IP>:5000`
- 公网：ngrok 穿透或云服务器部署

---

## 六、微信小程序启动方式

### 6.1 项目结构

```
Weapp/
├── app.js / app.json / app.wxss
├── pages/index/
│   ├── index.wxml    ← 卡片式 UI（温度/湿度/CO2/光照/土壤/火光+阈值+开关）
│   ├── index.js      ← 每 3 秒调 API 刷新数据
│   └── index.wxss    ← 样式
└── images/           ← 图标
```

### 6.2 启动步骤

1. **打开项目**：微信开发者工具 → 导入 → 选 `Weapp/` 目录 → AppID 用测试号
2. **本地调试**：
   - `index.js` 中 `useCloud: false`
   - `API_BASE` 填 `http://<PC_IP>:5000`
   - 勾选「不校验合法域名」
   - 编译 → 真机预览
3. **正式上线**：Flask 部署公网 + HTTPS + 域名白名单

---

## 七、RTOS 任务列表

| 任务 | 函数 | 优先级 | 栈大小 | 说明 |
|------|------|--------|--------|------|
| WiFi | `wifi_task_func` | High(13) | 8KB | 连接热点 + DHCP |
| HTTP | `http_server_task` | 18 | 8KB | 提供 Web 和 API |
| MQTT | `mqtt_task` | Normal | 12KB | 每 5 秒上报云端 |
| LED | `led_control_task` | Normal | 4KB | 呼吸灯/频闪 |
| SGH | `sgh_task` | 17 | 4KB | OLED + 传感器 + 蜂鸣器 + 按钮 |
| Button | `button_task` | 18 | 1KB | GPIO 扫描消抖 |

---

## 八、数据流向总结

```
传感器采集 (SGH任务)
     │
     ├──→ OLED 显示 (本地)
     ├──→ 蜂鸣器告警 (本地)
     ├──→ LED 指示 (LED任务)
     ├──→ server.c HTTP API ──→ app.py Flask ──→ 网页 / 小程序
     └──→ MQTT.c ──→ 华为云 IoTDA ──→ 设备影子 ──→ 云平台可视化
                                 │
                                 └──→ app.py (IAM鉴权读取) ──→ 网页
```

---

## 九、编译与烧录

1. 确认 `CONFIG_ENABLE_SMART_GREEN_HOUSE_SAMPLE=y` 已在构建配置中启用
2. 在工程根目录编译
3. 使用 HiBurn 工具通过串口烧录 `.bin` 文件
4. 波特率 115200，串口监视器查看运行日志

---

## 十、注意事项

- **DHT11 时序严格**：禁用中断会提高读取成功率
- **JW01 预热**：上电后需等待 2-3 分钟数据才稳定
- **华为云凭证过期**：ClientId 中的时间戳有效期为 24 小时，需用参数生成工具刷新
- **ADC 换算**：土壤和光照的线性公式为近似值，精确使用需标定
- **LED 引脚复用**：绿色 LED 与蜂鸣器复用 GPIO_02，两者互斥使用
